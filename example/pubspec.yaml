name: loader_pro_kit_example
description: "A new Flutter application."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  loader_pro_kit:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
